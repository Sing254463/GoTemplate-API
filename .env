# ============================================
# การตั้งค่าฐานข้อมูล (Database Configuration)
# ============================================
# ที่อยู่เซิร์ฟเวอร์ฐานข้อมูล - ใช้ localhost สำหรับการพัฒนาในเครื่อง
DB_HOST=localhost

# หมายเลขพอร์ตของ MySQL - 3306 เป็นพอร์ตมาตรฐานของ MySQL
DB_PORT=3306

# ชื่อผู้ใช้สำหรับเข้าสู่ฐานข้อมูล - root คือ admin ของ MySQL
DB_USER=root

# รหัสผ่านสำหรับเข้าสู่ฐานข้อมูล - ควรเป็นรหัสที่ปลอดภัย
DB_PASSWORD=your_password

# ชื่อฐานข้อมูลที่จะใช้งาน - your_DB คือชื่อฐานข้อมูลของเรา
DB_NAME=your_DB

# ============================================
# การตั้งค่า JWT (JSON Web Token)
# ============================================
# กุญแจลับสำหรับเซ็น JWT Token - ควรเป็น UUID หรือ string ที่ปลอดภัย
# ใช้สำหรับการเข้ารหัสและถอดรหัส JWT Token
JWT_SECRET=your-super-secret-jwt-key-here

# ระยะเวลาที่ JWT Token จะหมดอายุ
# 24h = 24 ชั่วโมง, สามารถใช้ 30m (30 นาที), 1h (1 ชั่วโมง) ได้
JWT_EXPIRE=24h

# ============================================
# การตั้งค่าเซิร์ฟเวอร์ (Server Configuration)
# ============================================
# หมายเลขพอร์ตที่เซิร์ฟเวอร์จะทำงาน - 8080 เป็นพอร์ตมาตรฐานสำหรับการพัฒนา
PORT=8080

# สภาพแวดล้อมการทำงาน - development (การพัฒนา), production (ใช้งานจริง)
ENVIRONMENT=development

# ============================================
# การตั้งค่าแอปพลิเคชัน (Application Configuration)
# ============================================
# ชื่อแอปพลิเคชัน
APP_NAME=GoTemplate API

# เวอร์ชันของแอปพลิเคชัน
APP_VERSION=1.0.1

# คำอธิบายแอปพลิเคชัน
APP_DESCRIPTION=Go API template with authentication and user management system. Perfect for quick project setup and development.